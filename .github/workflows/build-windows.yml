name:  MoveTK-CI-Windows

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build-windows:
    env:
      vcpkgCommitId: acc3bcf76b84ae5041c86ab55fe138ae7b8255c7
    runs-on: windows-latest

    steps:
      # We need a deeper checkout for now
      # https://github.com/microsoft/vcpkg/issues/25349
      - name: Checking out Vcpkg
        uses: actions/checkout@v2
        with:
            repository: microsoft/vcpkg
            path: vcpkg
            ref: '${{env.vcpkgCommitId}}'
            submodules: recursive
            fetch-depth: 0

     
      # Checkout repository
      - name: Checking out MoveTk
        uses: actions/checkout@v2
        with:
            repository: movetk/movetk
            submodules: recursive
            path: movetk   
     
      # Get the ninja generator
      - uses: seanmiddleditch/gha-setup-ninja@v3

      - name: Restore artifacts and/or setup vcpkg
        uses: lukka/run-vcpkg@v10
        with:
          prependedCacheKey: 'movetk-vcpkg'
          vcpkgDirectory: '${{ github.workspace }}/vcpkg'
          vcpkgGitCommitId: '${{env.vcpkgCommitId}}'
          vcpkgJsonGlob: 'movetk/vcpkg.json'

      # Build movetk
      - uses: seanmiddleditch/gha-setup-vsdevenv@master
      - name: Configure CMake with Ninja
        id: runcmake
        run: >
          cmake -S ${{github.workspace}}/movetk -B ${{github.workspace}}/build -GNinja
          -DCMAKE_TOOLCHAIN_FILE=${{ github.workspace }}/vcpkg/scripts/buildsystems/vcpkg.cmake
          -DMOVETK_WITH_CGAL_BACKEND=OFF
          -DMOVETK_BUILD_TESTS=ON
          -DMOVETK_BUILD_EXAMPLES=ON
          -DVCPKG_INSTALL_OPTIONS=--clean-after-build
      - name: Build MoveTk
        run: cmake --build .
        working-directory: ${{github.workspace}}/build
      - name: Run tests
        run: ctest . 
        working-directory: ${{github.workspace}}/build
