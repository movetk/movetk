name:  MoveTK-CI-Windows

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - uses: seanmiddleditch/gha-setup-ninja@master
    - name: list directory
      run: dir "C:\Program Files (x86)\Microsoft Visual Studio\2017\*"
    # 
    #- name: install dependencies
    #  run: C:\vcpkg\vcpkg.exe install boost gdal gsl cgal geographiclib --triplet x64-windows
    #- name: build movetk
    #  run: .\ci.cmd 	 
